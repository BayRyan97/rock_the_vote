import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function MenuScreen() {
  return (
    <ScreenPlaceholder
      title="Menu"
      task="P1-03"
      blurb="Account, the active build variant, help, and sign out."
    />
  );
}
