import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProgressScreen() {
  return (
    <ScreenPlaceholder
      title="Progress"
      task="P1-13"
      blurb="Doors attempted and contacted, time against the shift budget, sync state, and End Shift."
    />
  );
}
