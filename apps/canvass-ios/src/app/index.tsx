import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function MyListScreen() {
  return (
    <ScreenPlaceholder
      title="My List"
      task="P1-12"
      blurb="Map and list of the turf: clustered pins coloured by door state, filters, sorted in walking order."
    />
  );
}
