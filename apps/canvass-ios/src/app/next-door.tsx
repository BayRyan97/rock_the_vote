import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function NextDoorScreen() {
  return (
    <ScreenPlaceholder
      title="Next Door"
      task="P2-09"
      blurb="The route timeline: numbered stops, the next three pinned, teammate toasts when a door is taken."
    />
  );
}
